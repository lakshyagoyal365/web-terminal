import { type HTMLAttributes } from "react";
import { WTerm, type TerminalCore } from "@wterm/dom";
export interface TerminalProps extends Omit<HTMLAttributes<HTMLDivElement>, "onResize" | "onError"> {
    cols?: number;
    rows?: number;
    /**
     * A pre-constructed terminal core. When provided, `wasmUrl` is ignored and
     * this core is used instead of loading the built-in Zig WASM binary.
     */
    core?: TerminalCore;
    wasmUrl?: string;
    theme?: string;
    autoResize?: boolean;
    cursorBlink?: boolean;
    /** Enable debug mode (init-only — changing after mount has no effect). */
    debug?: boolean;
    onData?: (data: string) => void;
    onTitle?: (title: string) => void;
    onResize?: (cols: number, rows: number) => void;
    onReady?: (wt: WTerm) => void;
    onError?: (error: unknown) => void;
}
export interface TerminalHandle {
    write(data: string | Uint8Array): void;
    resize(cols: number, rows: number): void;
    focus(): void;
    readonly instance: WTerm | null;
}
declare const Terminal: import("react").ForwardRefExoticComponent<TerminalProps & import("react").RefAttributes<TerminalHandle>>;
export default Terminal;
//# sourceMappingURL=Terminal.d.ts.map